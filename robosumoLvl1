>>> from myro import*
>>> init(COM'number'")
>>> def roboSumoLvl1():
	while(not getstall()and getLine()<1):
		x=getIR("left")
		y=getIR("right")
		if(x=1 and y =0):
			motors(0.25,1)
		elif(y=1 and x=0):
			motor(1,0.25
		elif(sum(y,z)=1
			motots(1,1)
		else:
		turnLeft(1)
	while(not getstall() and getLine()=1:
		backward(1,1)
